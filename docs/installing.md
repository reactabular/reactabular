Reactabular is granular by default and you will have to choose which packages to install (`npm install` or `npm i`) yourself. The following image lists the major ones:

![Reactabular packages](./images/overall.png)
